apiVersion: apps/v1
kind: Deployment
metadata:
  name: uniborrow-users-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: uniborrow-users
  template:
    metadata:
      labels:
        app: uniborrow-users
    spec:
      containers:
        - image: lh5107/uniborrow-users:2021-12-02-01-33-36
          name: image-catalog
          env:
            - name: KUMULUZEE_DATASOURCES0_CONNECTIONURL
              value: jdbc:postgresql://surus.db.elephantsql.com:5432/jsyabbvk
            - name: KUMULUZEE_DATASOURCES0_USERNAME
              value: jsyabbvk
            - name: KUMULUZEE_DATASOURCES0_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: uniborrow-users-db-pass
                  key: password
            - name: KUMULUZEE_CONFIG_CONSUL_AGENT
              value: http://consul-consul-server:8500
          ports:
            - containerPort: 8080
              name: server
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health/live
              port: server
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health/ready
              port: server
            initialDelaySeconds: 30
            periodSeconds: 10
---

apiVersion: v1
kind: Service
metadata:
  name: uniborrow-users
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      protocol: TCP
      targetPort: server
  selector:
    app: uniborrow-users
